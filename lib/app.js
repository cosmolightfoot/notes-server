const express = require('express');
const app = express();

app.use(express.json());

// app.use('/api/v1/notes', require('./routes/notes.js'));

//todo add not found and error handling middleware



modules.export = {
  app
};
